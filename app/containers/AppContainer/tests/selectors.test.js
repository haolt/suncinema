// import { selectAppContainerDomain } from '../selectors';

describe('selectAppContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
