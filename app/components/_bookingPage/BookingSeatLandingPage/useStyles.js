import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  landingPage: {
    height: '92vh',
  },
  banner: {
    maxHeight: '100%', 
  }
}));
export default useStyles;
